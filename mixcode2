#include<iostream>
#include<fstream>
using namespace std;

void printArray(int str[], int n) {
    cout<<"Printing array element \n";
    for(int i=0; i<n; i++){
      cout<<str[i]<<endl;
    }
  }
  int main()
{
  ofstream newfile("C:\\Users\deepu\OneDrive\Desktop\cpp.txt");

  cout<<"File has Created ....";

  newfile<<"Hello Bhai....";
  newfile.close();
  int i,n;
  cout<<"Enter the Number :: ";
  cin>>n;
  for(i=2;i<=n;i+=2)
    {
      cout<<i<<endl;
     
    }

  int i,n,j;
  cin>>n;
  for(i=2;i<n;i++){

  if(n/i=0){
    
  }
  else{
    cout<<i<<endl;
  }
    }

bool isPrime(int n)
{
      // Since 0 and 1 is not
      // prime return false.
      if(n == 1 || n == 0) return false;
   
      // Run a loop from 2 to n-1
      for(int i = 2; i < n; i++)
      {
        // if the number is divisible by i,
        // then n is not a prime number.
        if(n % i == 0) return false;
      }
      // Otherwise n is a prime number.
      return true;
}
int main()
{
    int N = 50;
 
      // Check for every number from 1 to N
      for(int i = 1; i <= N; i++)
      {
          // Check if current number is prime
          if(isPrime(i))
          {
            cout << i << " ";
          }
    }
 
    return 0;
}

int i,n;
  cout<<"Enter the number :: ";
  cin>>n;
  for(i=2; i<n; i++){
   
if(n%i==0 && n){
  cout<<i<<endl;
}
  
  }




int n=10;

int str[7]={0,2,3,4,1,2,6};
  
printArray(str, 5);
  

  int n;
int arr[10]={2,0,5,4,3};

  for(int i=0; i<10; i++){
    
  
  cout<<"The indexed data is "<<arr[i]<<endl;
    }



  Array code start from here......................
 for(int i=0; i<a; i++){

    cout<<deep[i];
  }
    
  return 0;
}
